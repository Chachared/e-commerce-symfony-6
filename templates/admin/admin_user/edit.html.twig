{% extends 'base-admin.html.twig' %}

{% block title %}{{ parent() }}Modifier l'administrateur{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            <h1>Modifier l'administrateur</h1>
        </div>
        <div class="col-6 d-flex justify-content-end align-items-center">
            <a href="{{ path('admin_user_index')}}" class="btn btn-dark rounded-pill">Retour</a>
        </div>
    </div>
    {{ include('admin/admin_user/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('admin/admin_user/_delete_form.html.twig') }}
{% endblock %}
