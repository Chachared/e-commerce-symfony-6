<form method="post" action="{{ path('admin_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette marque?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger rounded-pill my-2">Supprimer</button>
</form>
