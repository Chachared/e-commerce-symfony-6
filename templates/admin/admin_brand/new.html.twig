{% extends 'base-admin.html.twig' %}

{% block title %}New Brand{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-8">
                <h1 class="mb-3 ms-4">Ajouter une nouvelle marque</h1>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
                <a href="{{ path('admin_brand_index') }}" class="btn btn-dark rounded-pill">Retour</a>
            </div>
        </div>
        <div class="row">
            {{ include('admin/admin_brand/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
